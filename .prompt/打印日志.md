使用git查看codex-rs/codex-api/src/endpoint/streaming.rs这个文件改动。这个文件加了日志写入磁盘的功能。

我使用这样的shell执行了cli
```
cargo run --manifest-path ~/workspace/codex/codex-rs/Cargo.toml \
  -p codex-cli -- --sandbox workspace-write \
  -C ~/workspace/marketing-x6
```

我的输入prompt是
```
新增一个商户号的规则中心的低码组件
```

问题：
1. 我找不到codex_prompt.log文件。不知道是没写入磁盘成功还是文件位置不对